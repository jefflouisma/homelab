apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: identity-keycloak-config
  namespace: argocd
spec:
  project: default
  source:
    repoURL: git@github.com:jefflouisma/homelab.git
    targetRevision: HEAD
    path: homepractice/apps/identity/keycloak
  destination:
    server: https://kubernetes.default.svc
    namespace: identity
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
