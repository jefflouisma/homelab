# Terraform files (all environments)
**/.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
*.tfvars

# Kubeconfig files (sensitive)
kubeconfig-*.yaml

# Legacy terraform directory (to be removed after migration)
terraform/

# Sensitive files - NEVER commit these
*.pem
*.key
argocd-deploy-key
argocd-deploy-key.pub
id_rsa
id_rsa.pub
g913-k8s-prod-arc*.pem

# OS files
.DS_Store
