# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Secrets - NEVER commit these
*.key
*.pem
*.crt
!secrets/.sops.yaml  # Allow sops config
.env
.env.*

# Exception: Allow public keys (they're meant to be shared)
!*.pub
id_rsa*

# Temporary files
*.tmp
*.log

# Local overrides
*.local.yaml
*.local.yml

.env 
# Rust build artifacts
moonlight-mcp/target/

# NixOS / Nix Flakes
result
result-*
.direnv/
.envrc

# sops-nix age keys (keep these local, never commit)
*.age
keys.txt

# Sensitive files (local secrets)
id_rsa
g913-k8s-prod-arc.*.private-key.pem
barleta/local.yaml
