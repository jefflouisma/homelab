# RetroArch Configuration for EmuDeck/Fenrir Streaming
# Called by ES-DE when launching RetroArch cores
# Aligned with proven settings from fenrir RetroArch apps.yaml

# Video settings - Wayland + Vulkan for NVIDIA
video_driver = "vulkan"
video_context_driver = "wayland"
video_fullscreen = "true"
video_vsync = "true"
video_frame_delay = "0"

# Let compositor control resolution (adapts to Moonlight client)
video_windowed_fullscreen = "true"

# Audio settings for PulseAudio streaming
audio_driver = "pulse"
audio_latency = "64"
audio_sync = "true"

# Input settings - SDL2 for reliable controller input
# linuxraw has KNOWN BUG where devices detected but input doesn't register
# SDL2 properly handles input events and works with virtual controllers
input_driver = "sdl2"
input_autodetect_enable = "true"
input_joypad_driver = "sdl2"
joypad_autoconfig_dir = "/usr/share/retroarch/autoconfig"
gamemode_enable = "false"

# Directories - EmuDeck folder structure
savefile_directory = "/Emulation/saves/retroarch/saves"
savestate_directory = "/Emulation/saves/retroarch/states"
screenshot_directory = "/screenshots"
system_directory = "/Emulation/bios"
rgui_browser_directory = "/Emulation/roms"

# Core directories
libretro_directory = "/usr/lib/libretro"
libretro_info_path = "/usr/share/retroarch/info"
content_database_path = "/usr/share/retroarch/database/rdb"
assets_directory = "/usr/share/retroarch/assets"

# Playlist directory
playlist_directory = "/Emulation/storage/retroarch/playlists"

# Auto-load core overrides and configs
auto_overrides_enable = "true"
auto_remaps_enable = "true"

# When loading content, automatically select matching core
load_dummy_on_core_retro = "false"

# Menu settings
menu_driver = "ozone"
menu_show_online_updater = "false"
menu_show_core_updater = "false"

# UI Scaling (DPI-based, responsive to screen size)
menu_scale_factor = "1.0"

# Disable analytics
analytics_enabled = "false"

# Wayland/GPU compatibility
video_shared_context = "false"
driver_switch_enable = "false"

# Logging - enabled for Grafana/Loki debugging
log_verbosity = "true"
log_to_file = "false"

# Quit RetroArch cleanly to return to ES-DE
quit_press_twice = "false"
