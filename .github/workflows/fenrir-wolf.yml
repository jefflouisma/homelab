name: "Build Fenrir Wolf"

on:
  push:
    branches: [main]
    paths:
      - "fenrir/images/wolf/**"
      - ".github/workflows/fenrir-wolf.yml"
  workflow_dispatch: {}

permissions:
  contents: write
  packages: write

concurrency:
  group: fenrir-wolf-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-update:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Tag
        id: vars
        run: echo "tag=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Set up buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build/push fenrir-wolf
        uses: docker/build-push-action@v6
        with:
          context: fenrir/images/wolf
          file: fenrir/images/wolf/docker/wolf.Dockerfile
          tags: ghcr.io/jefflouisma/fenrir-wolf:${{ steps.vars.outputs.tag }}
          push: true
          cache-from: type=gha,scope=fenrir-wolf
          cache-to: type=gha,mode=max,scope=fenrir-wolf

      - name: Update GitOps manifests
        run: |
          TAG="${{ steps.vars.outputs.tag }}"
          perl -pi -e 's|(ghcr.io/jefflouisma/fenrir-wolf:)[^[:space:]]+|${1}'"${TAG}"'|g' barleta/apps/house/fenrir/wolf-operator.yaml

      - name: Commit manifest updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add barleta/apps/house/fenrir/wolf-operator.yaml
          if git diff --cached --quiet; then
            echo "No manifest changes to commit."
            exit 0
          fi
          git commit -m "chore(fenrir): bump wolf to ${{ steps.vars.outputs.tag }}"
          git push
