# Encrypted secrets (sops-nix)
# This file is encrypted with age/gpg
# Edit with: sops secrets/secrets.yaml

# Placeholder - will be encrypted after running:
#   sops --encrypt --in-place secrets/secrets.yaml

keycloak:
  admin_password: ENC[AES256_GCM,placeholder]

argocd:
  admin_password: ENC[AES256_GCM,placeholder]

github:
  deploy_token: ENC[AES256_GCM,placeholder]
